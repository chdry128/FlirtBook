/*import 'package:flutter/material.dart';
import 'package:webview_flutter/webview_flutter.dart';

class AdWebViewScreen extends StatelessWidget {
  final String adUrl;

  AdWebViewScreen({required this.adUrl});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Adsteera Ad'),
      ),
      body: WebView(
        initialUrl: adUrl,
        javascriptMode: JavascriptMode.unrestricted,
      ),
    );
  }
}

 */
